@{
    ViewBag.Title = "Business Dashboard";
}
<div class="row income-orders">
    <div class="col-lg-3">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <span class="label label-info pull-right">Today</span>
                <h5>Orders</h5>
            </div>
            <div class="ibox-content">
                <a href="@Url.Action("BusinessOrders", "BusinessOrder")"><h1 class="no-margins">@ViewBag.OrdersToday</h1></a>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <span class="label label-danger pull-right">Monthly</span>
                <h5>Orders</h5>
            </div>
            <div class="ibox-content">
                <a href="@Url.Action("BusinessOrders", "BusinessOrder")">
                    <h1 class="no-margins">@ViewBag.OrdersMonth</h1>
                </a>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <span class="label label-info pull-right">Total</span>
                <h5>Available Drivers</h5>
            </div>
            <div class="ibox-content">
                <a><h1 class="no-margins">@ViewBag.OnlineDriversCount</h1></a>
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="col-lg-12 no-padding m-b-lg dropdown-alerts">
            <div class="m-t-md">
                <div class="panel widget no-padding">
                    <div class="panel-heading label-danger">
                        <h3>
                            <span><i class="glyphicon glyphicon-list"></i></span>
                            <span> Alerts </span>
                        </h3>
                    </div>
                    <div class="mCustomScrollbar panel-body table-responsive">
                        <ul class="list-wrapper">
                            @*@foreach (var item in ViewBag.TodoList)
                                {
                                    <li>
                                        <div class="menu-icon">
                                            <a href="@item.Link">
                                                <img src="@item.Image">
                                            </a>
                                        </div>
                                        <div class="menu-text @item.ItemColor">
                                            @item.Text
                                        </div>
                                        <div class="menu-text">
                                            <div class="menu-info">
                                                <a href="@item.Link">@item.ItemName</a>
                                            </div>
                                        </div>
                                    </li>
                                }*@
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>


@section Scripts {
    @Scripts.Render("~/plugins/flot")
    @Scripts.Render("~/plugins/chartJs")
    @Scripts.Render("~/plugins/peity")
    @Scripts.Render("~/bundles/BusinessDashboard")
}